# 第2天：变量与数据类型

## 🎯 今日目标

- 理解 Rust 变量的可变性概念
- 掌握基本数据类型
- 学会使用类型注解
- 了解常量和静态变量

## 🔧 变量与可变性

### 变量的基本概念

在 Rust 中，变量默认是**不可变**的，这是 Rust 的一个重要设计原则。

```rust
fn main() {
    let x = 5;        // 不可变变量
    println!("x = {}", x);
    
    // x = 6;         // 这行代码会编译错误！
    // 错误信息：cannot assign twice to immutable variable `x`
}
```

### 可变变量

使用 `mut` 关键字可以声明可变变量：

```rust
fn main() {
    let mut x = 5;    // 可变变量
    println!("x = {}", x);
    
    x = 6;            // 现在可以修改了
    println!("x = {}", x);
}
```

### 变量遮蔽（Shadowing）

Rust 允许使用相同的名字声明新变量，这叫做"遮蔽"：

```rust
fn main() {
    let x = 5;
    println!("x = {}", x);  // 输出：x = 5
    
    let x = x + 1;          // 遮蔽，x 现在是 6
    println!("x = {}", x);  // 输出：x = 6
    
    let x = x * 2;          // 再次遮蔽，x 现在是 12
    println!("x = {}", x);  // 输出：x = 12
}
```

**注意**：遮蔽与可变性不同，遮蔽创建了一个新变量，只是名字相同。

## 📊 基本数据类型

### 1. 整数类型

| 长度 | 有符号 | 无符号 |
|------|--------|--------|
| 8-bit | `i8` | `u8` |
| 16-bit | `i16` | `u16` |
| 32-bit | `i32` | `u32` |
| 64-bit | `i64` | `u64` |
| 128-bit | `i128` | `u128` |
| arch | `isize` | `usize` |

```rust
fn main() {
    let a: i32 = 42;        // 有符号 32 位整数
    let b: u32 = 42;        // 无符号 32 位整数
    let c = 42;             // 类型推断，默认是 i32
    
    // 字面量表示
    let decimal = 98_222;   // 98,222
    let hex = 0xff;         // 255
    let octal = 0o77;       // 63
    let binary = 0b1111_0000; // 240
    let byte = b'A';        // 65 (u8)
}
```

### 2. 浮点类型

Rust 有两种浮点类型：`f32` 和 `f64`（默认）。

```rust
fn main() {
    let x = 2.0;     // f64
    let y: f32 = 3.0; // f32
    
    // 数学运算
    let sum = x + y as f64;  // 类型转换
    let difference = x - y as f64;
    let product = x * y as f64;
    let quotient = x / y as f64;
    
    println!("sum: {}, diff: {}, product: {}, quotient: {}", 
             sum, difference, product, quotient);
}
```

### 3. 布尔类型

```rust
fn main() {
    let t = true;
    let f: bool = false;
    
    // 布尔运算
    let and = t && f;        // false
    let or = t || f;         // true
    let not = !t;            // false
    
    println!("and: {}, or: {}, not: {}", and, or, not);
}
```

### 4. 字符类型

Rust 的 `char` 类型是 Unicode 标量值，占 4 字节：

```rust
fn main() {
    let c = 'z';
    let z = 'ℤ';
    let heart_eyed_cat = '😻';
    
    println!("c: {}, z: {}, cat: {}", c, z, heart_eyed_cat);
}
```

## 🏷️ 类型注解

虽然 Rust 支持类型推断，但有时需要显式指定类型：

```rust
fn main() {
    // 类型推断
    let x = 5;               // 推断为 i32
    
    // 显式类型注解
    let y: i64 = 5;
    let z: f64 = 3.14;
    let flag: bool = true;
    
    // 函数参数需要类型注解
    let result = add(5, 3);
    println!("5 + 3 = {}", result);
}

fn add(a: i32, b: i32) -> i32 {
    a + b
}
```

## 🔒 常量与静态变量

### 常量（Constants）

常量使用 `const` 关键字声明，必须指定类型：

```rust
const MAX_POINTS: u32 = 100_000;
const PI: f64 = 3.14159;

fn main() {
    println!("最大点数: {}", MAX_POINTS);
    println!("圆周率: {}", PI);
}
```

### 静态变量（Static Variables）

静态变量使用 `static` 关键字，生命周期是整个程序：

```rust
static PROGRAM_NAME: &str = "Rust Tutorial";
static mut COUNTER: u32 = 0;

fn main() {
    println!("程序名称: {}", PROGRAM_NAME);
    
    // 修改静态变量需要 unsafe 块
    unsafe {
        COUNTER += 1;
        println!("计数器: {}", COUNTER);
    }
}
```

## 💻 动手实践

### 练习 1：类型转换

创建一个程序，演示不同类型的转换：

```rust
fn main() {
    let integer = 42;
    let float = 3.14;
    
    // 整数转浮点
    let int_to_float = integer as f64;
    
    // 浮点转整数（会截断）
    let float_to_int = float as i32;
    
    // 字符转数字
    let char_to_num = 'A' as u8;
    
    println!("整数 {} 转浮点: {}", integer, int_to_float);
    println!("浮点 {} 转整数: {}", float, float_to_int);
    println!("字符 'A' 转数字: {}", char_to_num);
}
```

### 练习 2：计算器增强版

改进昨天的计算器，支持更多数据类型：

```rust
fn main() {
    let a: f64 = 10.5;
    let b: f64 = 3.2;
    
    println!("=== 浮点计算器 ===");
    println!("{} + {} = {:.2}", a, b, a + b);
    println!("{} - {} = {:.2}", a, b, a - b);
    println!("{} * {} = {:.2}", a, b, a * b);
    println!("{} / {} = {:.2}", a, b, a / b);
    println!("{} % {} = {:.2}", a, b, a % b);
    
    // 幂运算
    println!("{} 的 {} 次方 = {:.2}", a, b, a.powf(b));
}
```

### 练习 3：类型推断实验

```rust
fn main() {
    // 让 Rust 推断类型
    let x = 5;           // 推断为 i32
    let y = 3.14;        // 推断为 f64
    let z = true;        // 推断为 bool
    let c = 'A';         // 推断为 char
    
    println!("x 的类型: {}", std::any::type_name::<typeof(x)>());
    println!("y 的类型: {}", std::any::type_name::<typeof(y)>());
    println!("z 的类型: {}", std::any::type_name::<typeof(z)>());
    println!("c 的类型: {}", std::any::type_name::<typeof(c)>());
}
```

## 🔍 代码解释

### 类型转换语法
```rust
let value = 42 as f64;  // 整数转浮点
```
- `as` 是类型转换操作符
- 只能进行安全的类型转换

### 字面量语法
```rust
let decimal = 98_222;   // 下划线分隔符，提高可读性
let hex = 0xff;         // 十六进制
let binary = 0b1111;    // 二进制
```

### 常量 vs 静态变量
- **常量**：编译时常量，没有固定内存地址
- **静态变量**：有固定内存地址，生命周期是整个程序

## 📚 今日总结

今天我们学习了：
1. ✅ 变量的可变性和遮蔽概念
2. ✅ 整数、浮点、布尔、字符等基本数据类型
3. ✅ 类型注解和类型推断
4. ✅ 常量和静态变量的使用
5. ✅ 类型转换和字面量语法

## 🎯 明日预告

明天我们将学习 Rust 的控制流，包括：
- if 表达式
- 循环（loop、while、for）
- match 模式匹配
- 控制流表达式

## 💡 小贴士

- 使用 `cargo check` 检查类型错误
- 善用类型推断，但必要时显式指定类型
- 常量命名通常使用大写字母和下划线
- 类型转换要小心，确保转换是安全的

---

**恭喜你完成了第二天的学习！明天见！** 🎉
